# API Configuration
# Backend API base URL - Required
# Format: Must be a valid URL with http/https protocol
# Production requires HTTPS
VITE_API_BASE_URL=http://localhost:3000/api/v1

# API Version Configuration
# Format: Must match pattern ^v\d+$
# Example: v1, v2, etc.
VITE_API_VERSION=v1

# Authentication Token Storage Configuration
# Format: Alphanumeric with underscores only
# Production requires secure storage implementation
VITE_AUTH_TOKEN_KEY=auth_token
VITE_REFRESH_TOKEN_KEY=refresh_token
VITE_TOKEN_EXPIRY_KEY=token_expiry

# Monitoring Configuration
# Health check interval in milliseconds (5000-300000)
# Default: 30000 (30 seconds)
VITE_MONITORING_INTERVAL=30000

# Feature Flags
# Enable/disable monitoring functionality
# Values: true/false
VITE_MONITORING_ENABLED=true

# Environment Configuration
# Determines environment-specific security measures and optimizations
# Values: development, staging, production
VITE_ENVIRONMENT=development

# Security Notice:
# 1. In production, VITE_API_BASE_URL must use HTTPS
# 2. Token storage keys must use secure storage in production
# 3. Monitoring interval should be adjusted based on server capacity
# 4. Environment-specific validation rules are enforced